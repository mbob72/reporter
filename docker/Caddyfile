{
  email {$CADDY_EMAIL}
}

{$DOMAIN} {
  encode zstd gzip

  @api path /health /reports* /tenants* /generated-files* /report-runs*

  handle @api {
    reverse_proxy api:3000
  }

  handle {
    reverse_proxy web:4200
  }
}

import { createTheme } from '@mantine/core';

export const reportWebTheme = createTheme({
  fontFamily: '"IBM Plex Sans", "Segoe UI", sans-serif',
  headings: {
    fontFamily: '"Merriweather", Georgia, serif',
  },
  primaryColor: 'teal',
  defaultRadius: 'md',
});
